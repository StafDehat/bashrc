Remove all the hard-coded crap from the glance scripts
Make authtoken a capital A and TenantID a capital T.  Do Region R too.
Learn github markup and make my README.md not look like crap
Fix the alert suppression script to pull data out of the response
Change image import to use cloud files as source, instead of local file
